{
  "name": "occam-athletics",
  "version": "1.0.0",
  "description": "Sacramento California",
  "main": "index.html",
  "scripts": {
    "sass": "sass --style=compressed tachyons.scss css/tachyons.css",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:eleventy": "eleventy --serve",
    "watch:sass": "npm run sass -- --watch",
    "start": "npm-run-all sass --parallel watch:*",
    "build": "npm run sass && eleventy"
  },
  "keywords": [],
  "author": "Joshua Vaage",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "sass": "^1.26.0",
    "tachyons-sass": "^4.9.5"
  }
}
